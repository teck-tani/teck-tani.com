# 작업 로그

## 2026-09-20 — 신규 메뉴 약 120개 문서 확인

- 사용자 요청으로 로컬 Markdown·다운로드 자료와 실제 앱 GitHub 원격을 읽기 전용 검색했다.
- `teck-tani/dev-program`의 삭제된 `document/NEW_TOOLS_ROADMAP.md`를 삭제 직전 `5f22e3c3`에서 확인했다. 6/8 작성 신규 100종이며 6/16 `cad03df` 문서 정리에서 삭제된 파일이다.
- 현재 원격 `a9976eab`의 `docs/빙-테스트도구-키워드-맵.md`는 9/1 작성 TOP20이다. 두 원문을 `docs/reference/`에 보존했다.
- 두 표 120행에서 경로 중복 2개, 현재 72종 레지스트리에 있는 도구 4개를 확인했다. 다른 이름의 기존 기능과 의미 중복은 별도 검토가 필요하다.
- `docs/신규-메뉴-120개-검토-2026-09-20.md` 작성 및 기존 수익 계획에 후속 근거를 추가했다. 추천은 전체 후보 유지와 첫 3개 검증·개발·측정이다.
- 원격·앱 코드·광고·SEO·서버·검색 설정, 커밋·PUSH·배포 변경 없음. 수요·순위·도구별 수익을 새로 측정하지 않았다.

## 2026-09-20 — 새 PC 인수 확인 및 다음 점검 준비

- 작업 시각: 2026-09-20 13:09 KST까지 확인
- 범위: 읽기 전용 환경·접근 확인과 로컬 문서 기록
- 기준: 2026-09-20 오전 점검 인수인계. 아래 색인 수치와 운영값은 새 실측이 아니라 전달 기록을 재사용한다.

### 확인한 내용

- 현재 Codex 작업 폴더 `/Users/tani/teck-tani.com`은 Git 루트이고 `main`은 `origin/main`과 같으며, 확인 시작 시 작업 트리는 깨끗했다.
- 이 저장소는 `README.md`와 `dev-program.code-workspace`만 가진 사이트 소개용 저장소다. 워크스페이스가 가리키는 실제 앱 경로 `/Users/tani/dev-program`은 이 PC에 없다.
- 요청된 `AGENTS.md`, 기존 `document/WORK_LOG.md`, Google 색인 공통 문서·9/20 평가서·감사자료·모니터 스크립트는 로컬에서 발견되지 않았다. 따라서 해당 원본을 읽었다고 간주하지 않는다.
- Google Search Console 로그인 세션과 `sc-domain:teck-tani.com` 속성 개요 접근은 새 PC에서 성공했다. URL 검사, 실시간 테스트, 색인 요청, 사이트맵 제출은 하지 않았다.
- 홈서버 SSH는 알려지지 않은 ED25519 호스트 키 때문에 엄격한 검증 단계에서 중단됐다. 호스트는 응답했지만 로그인·키 인증 단계에는 도달하지 않았으므로 SSH 접근 성공 또는 계정 문제로 판정하지 않는다. 호스트 키를 저장하거나 검증을 완화하지 않았다.
- 기존 예약 `automation-2` 조회는 앱에서 카드를 렌더링해 ID 존재를 확인했다. 새 PC 로컬 예약 설정 폴더는 없고, 응답에서 상태·연결 대상이 기계 판독 가능한 형태로 제공되지 않아 담당 작업 이관 여부는 미확정이다. 예약은 생성·수정·중지하지 않았다.

### 재사용한 기준선과 관찰 공백

- 인수인계상 마지막 원본 관찰 종료: `2026-09-20T01:12:51+00:00` (2026-09-20 10:12:51 KST)
- 이 세션에서는 원본 로그를 읽지 못했다. 따라서 위 시각 이후는 확인되지 않은 로그 공백으로 유지한다.
- 9/20 오전 1차 평가 결과는 `docs/구글-색인-현재상태.md`에 출처를 구분해 기록했다. 전체 평가를 반복하지 않았다.

### 변경하지 않은 항목

- 앱·SEO·광고·robots/noindex·사이트맵·lastModified·서버·DNS·보안·검색 콘솔 설정
- 커밋, PUSH, 배포, 롤백, Vercel 재개
- AdSense·GA4·Bing·네이버 콘솔 조회 및 로그인

### 다음 작업

- 9/21~9/26 중 하루 1회 두 실험 URL의 색인 상태·canonical·실제 검색 재방문·오류만 간단 관찰한다.
- 원본 로그 확인 전에 홈서버 호스트 키 지문을 신뢰할 수 있는 기존 기록과 대조해야 한다.
- 실제 앱 저장소와 원본 문서·모니터 스크립트를 이 PC에 가져오는 범위는 사용자 승인 후 정한다. 현재 소개 저장소에 앱 소스가 있다고 가정하지 않는다.


## 2026-09-20 — 키보드 테스트 구현·후속 자동 추가 예약

- 사용자 요청으로 실제 앱을 `/Users/tani/dev-program`에 복제해 키보드 테스트 ko/en을 구현했다. 타입/ESLint/8개 모델 테스트/342페이지 빌드와 로컬 브라우저 입력·동시 입력·초점 종료·초기화·다크/모바일을 확인했다. CUA 화면 캡처 제한은 앱 로그에 구분했다.
- 비공개 앱 main에 `0575fe2` push 완료. 공개 URL 배포 대기 상태이며 확인 후 결과를 추가한다. 공개 소개 README는 신규 키보드와 이전 누락9개를 실제73종 레지스트리에 맞춰 준비했다.
- 현재 작업 heartbeat `teck-tani` ACTIVE 생성, 매주 일요일20시KST~11/1. 10/4마우스·10/18모니터, 중간주점검, 11/1성과평가/종료. 120개 일괄생성 없음·최소14일·검증실패보류·Mac/Codex실행필요. [일정](../docs/신규-도구-자동추가-일정-2026-09-20.md) 참조.

- 20:02 KST 공개 새 URL은404/이전배포a9976ea여서 공개완료로 처리하지 않았다. 이 Mac에 SSH config/known_hosts 없음, 기존관리브라우저세션도 없어 서버진행/실패미확정. 동일작업 heartbeat는1개만허용되어 별도첫배포확인예약생성은실패했으며, 기존 teck-tani를오늘20:15·21:15·22:15확인→성공또는마지막회차뒤주간복원하도록수정했다. Git재push/강제재배포/보안완화없음. 공개소개README는아직로컬준비상태로, 검증후동일예약이README만push한다.


## 2026-09-20 — 전체 비중복 메뉴 확장으로 정정·18종 추가

- 사용자는 “기존 기능과 중복된 항목만 제외한 120개 후보 전체”를 빠르게 구현하도록 범위를 정정했다. 이전 3종 한정·14일 대기·11/1 종료 계획은 폐기했다. 원본 120행을 실제 코드와 대조한 결과 기존20 + 통합4 + 고유 구현 대상96이다. 전체 행·근거·배치는 `docs/tool-expansion-backlog.json` 및 `docs/전체-신규도구-실행목록.md`로 추적한다.
- 오늘 키보드에 이어18개를 구현했다: sort-lines, find-replace, reverse-text, roman-numeral, hangul-jamo, chosung-maker, indent-converter, line-numbering, number-base-converter, number-to-korean, unicode-escape, unicode-inspector, morse-code, rot13-cipher(시저 포함), json-diff, markdown-table-generator, slugify, strip-html. 소스 커밋 `e5a9753c0dcb488185bb1722cd6540802ac14456`. 합계19개 구현, 남은77개. 아직 공개 완료 수와 같지 않다.
- `src/features/expansion`에 작은 변환기용 공통 UI·서버 SEO를 구성하되 각 도구의 실제 변환·검증·예제·한영 콘텐츠를 구분했다. 개별 정적 경로36개와 OG36개, 레지스트리91종, 사이트맵200URL을 연결했다. 수정일은 신규18종 및 실제 목록·소개가 바뀐 홈/텍스트·개발 카테고리/소개만 반영했다. 기존 Google 실험 본문과 날짜는 그대로다.
- 단위검사20개 PASS: 로마 숫자1~3999 및 현대 한글11172개 전수 왕복, 큰 정수·소수 정밀도, UTF8/UTF16/surrogate, TSV, 문자열·그래핌, Unicode slug 보존. JSON 비교에서 숫자 정밀도 손실·언더플로를 감지하고 거짓 동일 판정을 차단했다. HTML은 detached template에서 텍스트만 읽고 입력 마크업을 페이지에 삽입하지 않는다. 분석 이벤트는 slug/action만 보내며 입력·출력·옵션은 보내지 않는다.
- TypeScript·변경 파일 ESLint·프로덕션 빌드414/414·diff 검사 통과. 기존 ESLintIgnore/Browserslist 및 middleware 경고만 유지. 로컬 HTTP검사50개 PASS: 새36페이지·36OG,1h1,canonical/hreflang,index,4종JSONLD와화면일치,사이트맵200,기존핵심경로. 기존 번역82개 네임스페이스 바이트 유지; 기존 article차이는 신규 관련도구카드만 제거하면 이전공개SHA와 바이트 일치. 결과 `.cache/expansion-local-verification.json`, 재검사 `.cache/verify-expansion-release.py`.
- 광고/GA 외부요청을 CSP로 차단한 로컬 실브라우저에서18개 예제 결과 확인. JSON 정밀도 오류·HTML script/style 제외·영문HTML/Markdown·초기화·390px 가로넘침 없음·다크 입력배경#0f172a/글자#e2e8f0 확인. 준비 전 입력 방지 상태를 추가했다. CUA 마우스 클릭/스크린샷 호출은 도구 오류가 있어 Enter 키 조작과 DOM/기하/계산스타일로 확인했으며 스크린샷 육안 검증으로 주장하지 않는다. 한영 OG PNG는 파일로 렌더링해 육안 확인했다.
- 기존 heartbeat `teck-tani`를 “teck-tani 전체 신규 메뉴 완성”으로 변경했다. 매일09/15/21시KST, 쉬운8~12개·복잡한3~6개를 기본 묶음으로 가능한 만큼 지속한다. 모든77개 잔여를9/21~9/25 회차에 배정하고9/26부터 미완료·미공개분을 이어간다. 날짜는 목표이며 완료 상한/자동 중단일이 아니다. 전체 구현·공개 검증 완료시에만 종료한다. Mac과Codex실행 필요. 배포 미확인은 다음 독립 구현의 중단 사유로 삼지 않는다.
- 공개 /ko/keyboard-test는20:30경까지404/이전버전이었다. 새 Mac의 신뢰된 홈서버 접근·로그가 없어 진행/실패 원인은 미확정이다. 이번18개 코드는 검사 후 정상main반영 대상으로 준비했으며 공개 확인은 별도로 이어간다. 서버보안/DNS/광고/GSC설정 변경이나 인증 우회는 하지 않았다. 공개 소개 README는 실제 공개된 도구만 반영하며 로컬 수익·감사·백로그 자료는 공개하지 않는다.

- 후속 상태: 신규18종 코드 e5a9753 및 전체 일정 문서 e41967f를 원격 main에 push 완료했다. push 직후 공개 /ko/sort-lines는404로 아직 공개 확인 대기다. 공개 소개 README는91종과 일치하도록 로컬 준비만 했으며 push하지 않았다. 다음 회차는9/20 21:00KST이며 공개 확인과 미완료 큐를 이어간다.
