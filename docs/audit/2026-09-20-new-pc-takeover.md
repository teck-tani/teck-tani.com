# 2026-09-20 새 PC Google 색인 인수 감사 기록

확인 완료: 2026-09-20 13:09 KST  
범위: 로컬 프로젝트·자료 존재 여부, GSC 접근, SSH 사전 접근, 기존 예약 식별  
새 색인 실측: 없음

## 1. 로컬 프로젝트와 자료

### 새 확인

- 작업 경로: `/Users/tani/teck-tani.com`
- Git: `main...origin/main`, 확인 시작 시 clean
- 원격: `https://github.com/teck-tani/teck-tani.com.git`
- 추적 파일은 사이트 소개 `README.md`와 `dev-program.code-workspace`뿐이다.
- 워크스페이스가 참조하는 `/Users/tani/dev-program`은 존재하지 않는다.
- 로컬 전체 Git 객체에도 앱 소스와 요청된 색인 문서·진단 스크립트 이력이 없다.

### 누락 자료

- `AGENTS.md`
- 기존 `document/WORK_LOG.md`
- `docs/구글-색인-현재상태.md`
- `docs/구글-색인-점검일정.md`
- `docs/구글-색인-1차평가-2026-09-20.md`
- `docs/audit/2026-09-20-google-console.md`
- `docs/audit/2026-09-20-indexing-http-origin.md`
- `docs/홈서버-자동배포.md`
- 두 Google 색인 진단 스크립트

이 감사에서 새로 만든 공통 문서는 인수 확인용 로컬 기록이다. 누락된 이전 PC 원본 평가서·감사자료를 읽거나 복원한 것으로 취급하지 않는다.

## 2. 접근 확인

| 대상 | 결과 | 판정 |
|---|---|---|
| Google Search Console 로그인 | 로그인된 화면 로드 | 세션 만료 아님 |
| `sc-domain:teck-tani.com` | 속성 개요 로드 | GSC 접근 정상 |
| URL 검사·색인 요청 | 실행하지 않음 | 범위 준수 |
| 홈서버 SSH | 포트 응답 후 알 수 없는 ED25519 호스트 키에서 중단 | 네트워크 단절이나 계정 만료로 단정 불가; 인증 미시험 |
| `automation-2` | 앱에서 조회 카드 렌더링 | ID 존재 확인, 활성 상태·담당 작업 연결 미확정 |

SSH 시험은 `BatchMode=yes`, `StrictHostKeyChecking=yes`로 실행했다. 새 호스트 키를 저장하지 않았고 검증을 완화하지 않았다. 비밀키·쿠키·토큰·환경파일 값은 기록하지 않았다.

## 3. 측정 로그 구분

- 마지막 검증된 원본 로그 관찰 종료는 인수인계상 `2026-09-20T01:12:51+00:00`이다.
- 이 세션은 서버 원본 로그를 읽지 못했으므로 그 이후 요청 수, Googlebot 재방문, 오류 여부를 새로 측정하지 않았다.
- GSC에서는 접근 가능 여부만 확인했고, 개요 수치를 새 평가값으로 채택하지 않았다.
- 9/20 오전 평가 수치는 `docs/구글-색인-현재상태.md`에 `인수인계 재사용`으로만 보존했다.

## 4. 예약 확인 한계

- 새 PC에 `/Users/tani/.codex/automations` 폴더가 없다.
- 앱의 ID 조회는 성공했지만 상태·대상 작업이 도구 응답에 기계 판독 가능한 텍스트로 노출되지 않았다.
- 최근 작업 목록과 이 호스트의 보관 작업 목록에서 같은 이름의 별도 담당 작업은 확인되지 않았다.
- 따라서 예약이 이 새 세션으로 이관됐다고 가정하지 않으며, 중복 예약도 만들지 않는다.

## 5. 결론과 다음 조건

- 2026-09-20에 필요한 인수 확인은 완료했다.
- Google Search Console은 사용할 수 있다.
- 앱 소스·원본 문서·모니터 스크립트가 없어 9/21 관찰의 로컬 재현 기반은 아직 없다.
- 홈서버 로그를 보기 전 신뢰 가능한 기존 기록으로 SSH 호스트 키 지문을 대조해야 한다.
- 실제 앱 저장소 확보 또는 원본 문서 이전은 사용자 승인 범위를 받은 뒤 진행한다.
- 커밋·PUSH·배포·롤백·운영 설정 변경은 하지 않았다.
